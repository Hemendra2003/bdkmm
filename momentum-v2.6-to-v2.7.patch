diff --git a/index.html b/index.html
index 60b0df5..0c9601b 100644
--- a/index.html
+++ b/index.html
@@ -93,7 +93,7 @@ body{margin:0;padding:0;width:100vw;max-width:100%;height:100vh}
 .velocity-unit-center{font-family:var(--mono);font-size:11px;color:var(--slate2);letter-spacing:.25em;text-align:center;margin-top:6px}
 .h-badge span:not(.mult-dot){font-family:var(--pixel);font-size:13px;line-height:1;color:var(--green);text-shadow:0 0 10px rgba(61,255,110,.35)}
 .h-badge-num-row{display:inline-flex;align-items:center;gap:5px}
-.badge-emoji{font-size:20px;display:inline-flex;align-items:center;line-height:1;filter:sepia(1) saturate(6) hue-rotate(2deg) brightness(1.1);transform:translateY(-3px)}
+.badge-emoji{font-size:26px;display:inline-flex;align-items:center;line-height:1;filter:sepia(1) saturate(6) hue-rotate(2deg) brightness(1.1);transform:translateY(-3px)}
 @keyframes mpulse{0%,100%{opacity:1}50%{opacity:0}}
 .mult-dot{width:4px;height:4px;background:var(--green);border-radius:50%;display:inline-block;animation:mpulse 1.2s steps(2) infinite;flex-shrink:0}
 .mult-row{display:inline-flex;align-items:center;gap:5px}
@@ -108,7 +108,7 @@ body{margin:0;padding:0;width:100vw;max-width:100%;height:100vh}
 .h-badge-sep{width:1px;height:28px;background:var(--bg3)}
 
 /* DIVIDER */
-.divider{width:100%;max-width:var(--max);border:none;border-top:1px solid var(--bg3);margin:22px 20px;align-self:center}
+.divider{width:100%;max-width:var(--max);border:none;border-top:1px solid var(--bg3);margin:22px 0;align-self:center}
 
 /* SECTION LABEL */
 .section-label{width:100%;font-family:var(--mono);font-size:9px;letter-spacing:.22em;text-transform:uppercase;color:var(--slate2);margin-bottom:12px}
@@ -497,7 +497,7 @@ body{margin:0;padding:0;width:100vw;max-width:100%;height:100vh}
       <div class="hero-badges">
         <div class="h-badge" id="delta-badge" style="display:none">
           <span id="delta-badge-num">—</span>
-          <div class="h-badge-label">VS YESTERDAY</div>
+          <div class="h-badge-label">FROM YESTERDAY</div>
         </div>
         <div class="h-badge-sep" id="badge-sep-1" style="display:none"></div>
         <div class="h-badge" id="streak-badge">
@@ -527,7 +527,7 @@ body{margin:0;padding:0;width:100vw;max-width:100%;height:100vh}
     <hr class="divider">
 
     <!-- 7-day line graph -->
-    <div class="section-label">7-DAY Δv TREND</div>
+    <div class="section-label">7-DAY TREND</div>
     <div class="line-graph-wrap" style="margin-bottom:4px">
       <svg id="week-line-svg" class="line-graph-svg" height="200"></svg>
       <div class="lg-tooltip" id="week-tooltip"></div>
@@ -695,7 +695,7 @@ body{margin:0;padding:0;width:100vw;max-width:100%;height:100vh}
     <div class="streak-ribbon" id="sh-streak-ribbon"><span id="sh-streak-icon"></span><span id="sh-streak-text" style="font-family:var(--mono);font-size:11px">—</span></div>
     <div class="sum-compare-title">VS PREVIOUS DAYS</div>
     <div class="compare-grid" id="sh-compare"></div>
-    <div class="section-label" style="margin-top:4px;width:100%">THIS WEEK Δv</div>
+    <div class="section-label" style="margin-top:4px;width:100%">THIS WEEK TREND</div>
     <div class="line-graph-wrap" style="margin-bottom:4px">
       <svg id="sum-week-line-svg" class="line-graph-svg" height="120"></svg>
     </div>
@@ -1152,16 +1152,20 @@ function renderLineGraph(svgId, tooltipId, points, opts){
     }
     if(p.value===null) return;
     const y=yOf(p.value);
-    const col=opts&&opts.centreZero?(p.value>=0?'#3DFF6E':'#FF3030'):(opts&&opts.dotColor?opts.dotColor:'#FFB830');
-    svg.appendChild(mk('circle',{cx:x.toFixed(1),cy:y.toFixed(1),r:p.isToday?'4':'2.5',fill:col,stroke:p.isToday?'#05060D':'none','stroke-width':'2'}));
+    const baseCol=opts&&opts.dotColor?opts.dotColor:'#FFB830';
+    // today's dot+number: green if up from yesterday, red if down, white if no comparison
+    const todayCol=opts&&opts.todayTrendColor?opts.todayTrendColor:'#FFFFFF';
+    const dotCol=p.isToday?todayCol:baseCol;
+    const valCol=p.isToday?todayCol:baseCol;
+    svg.appendChild(mk('circle',{cx:x.toFixed(1),cy:y.toFixed(1),r:p.isToday?'4.5':'2.5',fill:dotCol,stroke:p.isToday?'#05060D':'none','stroke-width':'2'}));
     if(points.length<=10||p.isToday){
-      const valStr=(opts&&opts.centreZero&&p.value>=0?'+':'')+Math.round(p.value);
+      const valStr=Math.round(p.value);
       const vt=mk('text',{
         x:x.toFixed(1),y:(y-9).toFixed(1),'text-anchor':'middle',
-        fill:p.isToday?'#FFFFFF':col,
+        fill:valCol,
         'font-family':'DM Mono,monospace',
-        'font-size':p.isToday?'10':'8.5',
-        'font-weight':p.isToday?'600':'400'
+        'font-size':p.isToday?'11':'9',
+        'font-weight':p.isToday?'700':'400'
       });
       vt.textContent=valStr;svg.appendChild(vt);
     }
@@ -1181,7 +1185,9 @@ function renderWeekLineGraph(cache){
     value:(cache[k]&&cache[k].computed)?cache[k].computed.finalDv:null,
     isToday:k===tk,
   }));
-  renderLineGraph('week-line-svg','week-tooltip',points,{centreZero:true,lineColor:'rgba(255,184,48,.95)',fillColor:'rgba(255,184,48,.22)'});
+  const filledWk=points.filter(p=>p.value!==null);
+  const wkTrend=filledWk.length>=2?(filledWk[filledWk.length-1].value>filledWk[filledWk.length-2].value?'#3DFF6E':'#FF4444'):null;
+  renderLineGraph('week-line-svg','week-tooltip',points,{centreZero:true,lineColor:'rgba(255,184,48,.95)',fillColor:'rgba(255,184,48,.22)',dotColor:'#FFB830',todayTrendColor:wkTrend||'#FFFFFF'});
 }
 
 // MONTH LINE GRAPH — responsive: shows actual logged span, up to 30 days
@@ -1210,7 +1216,9 @@ function renderMonthLineGraph(cache){
       isToday:k===tk,
     };
   });
-  renderLineGraph('month-line-svg','month-tooltip',points,{centreZero:false,lineColor:'rgba(91,191,255,.9)',fillColor:'rgba(91,191,255,.2)',dotColor:'#5BBFFF'});
+  const filledMo=points.filter(p=>p.value!==null);
+  const moTrend=filledMo.length>=2?(filledMo[filledMo.length-1].value>filledMo[filledMo.length-2].value?'#3DFF6E':'#FF4444'):null;
+  renderLineGraph('month-line-svg','month-tooltip',points,{centreZero:false,lineColor:'rgba(91,191,255,.9)',fillColor:'rgba(91,191,255,.2)',dotColor:'#5BBFFF',todayTrendColor:moTrend||'#5BBFFF'});
 }
 
 // DASHBOARD RENDER
